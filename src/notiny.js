(function( $ ) {
  $.fn.notiny = function(text, options){
    var settings = $.extend({
      image: undefined,
      position: "bottom right",
      theme: "light"
    }, options);
  };
}( jQuery ));
